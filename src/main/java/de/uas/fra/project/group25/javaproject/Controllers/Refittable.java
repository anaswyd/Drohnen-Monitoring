package de.uas.fra.project.group25.javaproject.Controllers;

public interface Refittable {
    /**
     * Content should always be resizes to the window size different scenes require different implementations
     */
    void fitCenter();
}
